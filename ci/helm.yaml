apiVersion: apps/v1
kind: Deployment
metadata:
  name: helm
spec:
  selector:
    matchLabels:
      app: helm
  replicas: 1
  template:
    metadata:
      labels:
        app: helm
    spec:
      containers:
        - name: app
          image: localhost:32000/bookingcom/shipper-helm:latest
          env:
            - name: POD_IP
              valueFrom:
                fieldRef:
                  fieldPath: status.podIP

