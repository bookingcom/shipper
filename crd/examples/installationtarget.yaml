apiVersion: shipper.booking.com/v1
kind: InstallationTarget
metadata:
  name: install-v0.0.1 # a SHA1 or git tag from the CI/CD pipeline
  labels:
    release: reviewsapi-4
spec:
  clusters:
  - local
